// type Fn = (n: number, i: number) => any;

// function filter(arr: number[], fn: Fn): number[] {
//   let res: number[] = [];

//   arr.forEach((e, i) => {
//     if (fn(e, i)) {
//       res.push(e);
//     }
//   });
//   return res;
// }
